import OpenAI from "openai"; import { AIAnalysis, Position, Trade, Portfolio } from "./types"; const openai = new OpenAI({ apiKey: process.env.OPENAI_API_KEY, }); export async function analyzeTradingPerformance( positions: Position[], trades: Trade[], portfolio: Portfolio ): Promise<AIAnalysis> { try { const prompt = `Analyze this trading performance data and provide insights: Positions: ${JSON.stringify(positions)} Trades: ${JSON.stringify(trades)} Portfolio: ${JSON.stringify(portfolio)} Please provide: 1. A summary of trading performance 2. Key risk factors to watch 3. Specific improvement suggestions 4. A confidence score (1-10) 5. Any caution notes Format as JSON with keys: summary, riskFactors, improvements, confidenceScore, cautionNotes`; const completion = await openai.chat.completions.create({ model: "gpt-4", messages: [{ role: "user", content: prompt }], temperature: 0.3, }); const response = completion.choices[0]?.message?.content; if (!response) throw new Error("No response from OpenAI"); const analysis = JSON.parse(response); return { summary: analysis.summary || "Analysis unavailable", riskFactors: analysis.riskFactors || [], improvements: analysis.improvements || [], confidenceScore: analysis.confidenceScore || 5, cautionNotes: analysis.cautionNotes || [], }; } catch (error) { console.error("Error analyzing trading performance:", error); return { summary: "Unable to analyze trading performance at this time", riskFactors: ["Analysis failed"], improvements: ["Check system connectivity"], confidenceScore: 0, cautionNotes: ["AI analysis unavailable"], }; } }
