import { AIAnalysis as AIAnalysisType } from "../lib/types"; import { Brain, AlertTriangle, Lightbulb, Shield } from "lucide-react"; interface AIAnalysisProps { analysis: AIAnalysisType; } export default function AIAnalysis({ analysis }: AIAnalysisProps) { const getConfidenceColor = (score: number) => { if (score >= 8) return "text-green-600"; if (score >= 6) return "text-yellow-600"; return "text-red-600"; }; const getConfidenceLabel = (score: number) => { if (score >= 8) return "High"; if (score >= 6) return "Medium"; return "Low"; }; return ( <div className="bg-white rounded-lg border border-gray-200 p-4"> <h2 className="text-lg font-semibold text-gray-800 mb-4 flex items-center gap-2"> <Brain className="h-5 w-5 text-purple-600" /> AI Analysis </h2> <div className="space-y-4"> <div className="p-3 bg-blue-50 rounded-lg"> <h3 className="font-medium text-blue-800 mb-2">Performance Summary</h3> <p className="text-sm text-blue-700">{analysis.summary}</p> </div> <div className="grid grid-cols-1 gap-4 md:grid-cols-2"> <div className="p-3 bg-red-50 rounded-lg"> <h3 className="font-medium text-red-800 mb-2 flex items-center gap-2"> <AlertTriangle className="h-4 w-4" /> Risk Factors </h3> <ul className="text-sm text-red-700 space-y-1"> {analysis.riskFactors.map((factor, index) => ( <li key={index} className="flex items-start gap-2"> <span className="text-red-500">•</span> <span>{factor}</span> </li> ))} </ul> </div> <div className="p-3 bg-green-50 rounded-lg"> <h3 className="font-medium text-green-800 mb-2 flex items-center gap-2"> <Lightbulb className="h-4 w-4" /> Improvements </h3> <ul className="text-sm text-green-700 space-y-1"> {analysis.improvements.map((improvement, index) => ( <li key={index} className="flex items-start gap-2"> <span className="text-green-500">•</span> <span>{improvement}</span> </li> ))} </ul> </div> </div> <div className="p-3 bg-purple-50 rounded-lg"> <h3 className="font-medium text-purple-800 mb-2 flex items-center gap-2"> <Shield className="h-4 w-4" /> Confidence Score </h3> <div className="flex items-center gap-3"> <span className={`text-2xl font-bold ${getConfidenceColor(analysis.confidenceScore)}`}> {analysis.confidenceScore}/10 </span> <span className={`px-2 py-1 rounded text-xs font-medium ${getConfidenceColor(analysis.confidenceScore)} bg-opacity-20`}> {getConfidenceLabel(analysis.confidenceScore)} </span> </div> </div> {analysis.cautionNotes.length > 0 && ( <div className="p-3 bg-yellow-50 rounded-lg"> <h3 className="font-medium text-yellow-800 mb-2">Caution Notes</h3> <ul className="text-sm text-yellow-700 space-y-1"> {analysis.cautionNotes.map((note, index) => ( <li key={index} className="flex items-start gap-2"> <span className="text-yellow-500">•</span> <span>{note}</span> </li> ))} </ul> </div> )} </div> </div> ); }
