import { Position } from "../lib/types"; import { TrendingUp, TrendingDown, AlertTriangle } from "lucide-react"; interface OpenPositionsProps { positions: Position[]; } export default function OpenPositions({ positions }: OpenPositionsProps) { if (positions.length === 0) { return ( <div className="bg-white rounded-lg border border-gray-200 p-4"> <h2 className="text-lg font-semibold text-gray-800 mb-4">Open Positions</h2> <p className="text-gray-500 text-center py-8">No open positions</p> </div> ); } return ( <div className="bg-white rounded-lg border border-gray-200 p-4"> <h2 className="text-lg font-semibold text-gray-800 mb-4">Open Positions ({positions.length})</h2> <div className="space-y-3"> {positions.map((position) => { const isPositive = position.pnl >= 0; const isHighLeverage = position.leverage > 10; return ( <div key={position.symbol} className="border border-gray-200 rounded-lg p-3"> <div className="flex justify-between items-start mb-2"> <div> <h3 className="font-semibold text-gray-800">{position.symbol}</h3> <p className="text-sm text-gray-600">Size: {position.size}</p> </div> <div className="text-right"> <div className={`flex items-center gap-1 ${isPositive ? "text-green-600" : "text-red-600"}`}> {isPositive ? ( <TrendingUp className="h-4 w-4" /> ) : ( <TrendingDown className="h-4 w-4" /> )} <span className="font-semibold">₹{position.pnl.toLocaleString()}</span> </div> <p className="text-sm text-gray-600"> {position.pnlPercentage.toFixed(2)}% </p> </div> </div> <div className="grid grid-cols-2 gap-2 text-sm"> <div> <span className="text-gray-600">Entry:</span> <span className="font-medium">₹{position.entryPrice.toLocaleString()}</span> </div> <div> <span className="text-gray-600">Current:</span> <span className="font-medium">₹{position.markPrice.toLocaleString()}</span> </div> <div> <span className="text-gray-600">Leverage:</span> <span className={`font-medium ${isHighLeverage ? "text-red-600" : "text-gray-800"}`}> {position.leverage}x </span> </div> <div> <span className="text-gray-600">Margin:</span> <span className="font-medium">₹{position.margin.toLocaleString()}</span> </div> </div> {isHighLeverage && ( <div className="mt-2 p-2 bg-red-50 rounded flex items-center gap-2"> <AlertTriangle className="h-4 w-4 text-red-600" /> <span className="text-sm text-red-800">High leverage position</span> </div> )} </div> ); })} </div> </div> ); }
